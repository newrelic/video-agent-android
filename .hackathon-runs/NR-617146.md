# Run report: NR-617146

**Status:** done
**Branch:** fix/NR-617146-hls-content-bitrate-zero
**PR:** https://github.com/newrelic/video-agent-android/pull/187

## Timeline
- agile-ceremonies (hygiene check): FAILED — ticket had a one-line description, no acceptance criteria, no comments, no linked repo/PR/Slack thread. Router stopped per rule 2 and asked the user directly for the repo and what "done" looks like.
- (user-provided) repo: https://github.com/newrelic/video-agent-android; task: fix why contentBitrate comes back 0 for HLS streams.
- explore: root cause found — media3's `HlsMediaPeriod` tags the main variant's muxed segment loads as `C.TRACK_TYPE_DEFAULT`, not `C.TRACK_TYPE_VIDEO`, so `NRTrackerExoPlayer.onLoadCompleted`'s bitrate-assignment block (gated on `trackType == TRACK_TYPE_VIDEO`) never ran for HLS. DASH/progressive unaffected — their track typing comes from the manifest/extractor directly as `VIDEO`. Also found this had been attempted before (PR #118, closed unmerged) and re-shipped without a real fix in PR #124. No existing test coverage on this path.
- implementor (first pass): TDD — added `NRTrackerExoPlayerBitrateTest.java` (6 tests), confirmed 5 of them failed against the unfixed code (exact expected failure mode), then applied fix #1 (broadened trackType gate to accept `TRACK_TYPE_DEFAULT` when the `Format` actually carries video) and fix #2 (`getActualBitrate()` null-coalesces like its sibling getters). Committed to `fix/NR-617146-hls-content-bitrate-zero` (5880f2c) after retrying once due to a transient infrastructure error (unrelated to the code).
- code-review: PASS. Verified against the real media3 1.2.0 API (decompiled `Format`/`MediaLoadData`/`LoadEventInfo`) rather than trusting the diff's assumptions. Traced every caller of `getActualBitrate()` to confirm the null-consistency change is safe. Minor non-blocking notes: could have used media3's `MimeTypes.isVideo()` helper instead of a hand-rolled mime check; an HLS variant declaring neither `RESOLUTION` nor `CODECS` would still slip past the discriminator (narrow edge case).
- validator: PASS. Re-ran the test suite independently (`NRExoPlayerTracker`: 43/43; `NewRelicVideoCore`/`NRIMATracker`/`NRMediaTailorTracker`: 782/782, no regressions). `NRTHEOPlayerTracker`/`app` modules not verified — unrelated SSL/cert error reaching TheoPlayer's Maven repo in this sandbox, not caused by this change. Flagged: this repo's GitHub Actions don't trigger on `pull_request` events, so branch protection (outside this repo's tracked files) needs to be the actual test gate. Minor nit: commit uses `fix(android):` where repo convention is `fix(exoplayer):`.
- agile-ceremonies (closing update): posted summary comment to NR-617146 (comment ID 1887322).
- output: pushed branch, opened PR #187, wrote this report.

## Findings resolved
- Root gate bug (HLS loads tagged `TRACK_TYPE_DEFAULT`, never matched `TRACK_TYPE_VIDEO`) — fixed.
- `getActualBitrate()` returning misleading `0` instead of `null` when unset — fixed, brought in line with sibling getters.
- Zero test coverage on `onLoadCompleted`/bitrate tracking — closed with 6 new tests.

## Open items (not blocking, flagged for follow-up)
- No PR-triggered CI test run found in `.github/workflows` — confirm branch protection covers this.
- `NRTHEOPlayerTracker`/`app` modules unverified locally due to sandbox network limitation (unrelated to this fix).
- Commit type/scope nit: `fix(android)` vs. repo convention `fix(exoplayer)`.
